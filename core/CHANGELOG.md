# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Entries below this line are written by release-plz from conventional-commit
messages on `main`. Do not hand-edit released sections.

## [Unreleased]

## [0.1.0](https://github.com/SecurityRonin/peira/releases/tag/peira-core-v0.1.0) - 2026-08-20

### Added

- *(core)* GREEN — an instrument is a node, and measured_by reaches it
- *(lens)* GREEN — enforce the falsifier, promoting PREMORTEM
- vault loader and the deterministic lint pack
- *(lens)* the catalogue as data, and the eight enforced gates
- *(core)* edges, evidence grades, pramana ceilings, grounded extension
- *(core)* GREEN — refuse hand-written status and confidence
- *(core)* GREEN — parse a claim document into a typed Node

### Documentation

- the discipline peira mechanises, so the tool stands alone
- skills, README, ADRs, CI — and a false MSRV caught by its own gate

### Fixed

- *(core)* a frozen packet is not a node, and a replaced note stops binding
- *(core,lens)* a withdrawn attack is not in play, and a smaller kind test is still one
- *(cli,docs)* the front door opens, and twelve doc claims match the binary
- *(cli,core)* three commands agree, an empty vault is not a clean one
- *(core,lens,court)* an edge the argumentation discards has no effects anywhere
- *(core)* supersession is a version chain, not a liftable retraction
- *(core,lens,court)* sublation retires a claim, and volatility is not a verdict
- *(core,lens)* reference material does not compete, and the discard is reported
- rounds 4 and 5 — a rule lost at a join, four more times
- *(core,court,lens)* authored text may not impersonate structure
- *(core,court,cli,lens)* a retraction is an assertion, not a fact
- *(core,lens,cli)* a withdrawn claim does not argue, and status tells the truth
- *(core)* drop a stray #[test] left by the previous commit

### Other

- [**breaking**] rename the suite to peira

### Performance

- *(core)* compute the withdrawal fixed point once, not once per node
