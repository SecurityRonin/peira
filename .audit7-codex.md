## F1: `gates` exits successfully after reporting blocking findings
**Severity:** high
**Location:** cli/src/main.rs:243
**Reproduction:**

```text
$ target/debug/peira gates tests/vaults/overclaim; printf 'exit: %s\n' "$?"
✗ gates: 8 finding(s).

  PEIR-CRITERION-UNDECLARED [LIJI]  c-overclaim
      evaluative claim "This Amcache entry proves execution of the suspicious binary" declares no criterion
      → add a `judged_by:` edge to a Criterion stating the standard applied

  PEIR-GATE-UNASSESSED [ZHENGMING]  c-overclaim
      [ZHENGMING] reached no verdict: "This Amcache entry proves execution of the suspicious binary" declares no key terms, so which words are load-bearing is unknown — add `uses_term:`, or declare `no_terms_of_art: true` if it truly turns on none
      → supply what the gate needs, or say plainly that this claim was never examined for it — a gate that could not run has cleared nothing

  PEIR-FUNCTION-AS-SUBSTANCE [TIYONG]  c-overclaim
      substance claim "This Amcache entry proves execution of the suspicious binary" rests only on function evidence (o1, o2)
      → restate as a claim about what the thing did, or add evidence bearing on what it is

  PEIR-CLASS-EXTENSION-UNDECLARED [BAIMA]  c-overclaim
      claim quantifies `universal` over "This Amcache entry proves execution of the suspicious binary" without declaring its extension
      → declare `extension:` — what the class contains — or narrow the claim to the case actually examined

  PEIR-CORNERS-UNADDRESSED [CATUSKOTI]  c-overclaim
      contested claim "This Amcache entry proves execution of the suspicious binary" addresses 0 of 4 corners
      → state all four: A, not-A, both, neither — ruling one out with a reason counts as addressing it

  PEIR-WARRANT-MISSING [TOULMIN]  c-overclaim
      "This Amcache entry proves execution of the suspicious binary" states no warrant
      → write the rule that licenses the step from grounds to claim — it is usually the part that turns out to be false

  PEIR-CAUSAL-RUNG-UNREACHED [RUNG]  c-overclaim
      "This Amcache entry proves execution of the suspicious binary" claims the counterfactual rung but rests on observation alone — no executed protocol supports it
      → run a controlled protocol and cite the Run, or restate the claim at the association rung

  PEIR-BOUNDARIES-MISSING [RUNG]  c-overclaim
      "This Amcache entry proves execution of the suspicious binary" declares no boundary conditions
      → name the versions, configurations or populations where the claim holds — and cite each, never a bare string

exit: 0
```

**Why it matters:** Automation invoking `peira gates` sees success even though eight enforced checks block the vault. The human-readable report and process verdict contradict each other, allowing a failing claim to pass CI or any wrapper that relies on the documented exit status.

## Checked and clean

- Built `peira-cli` successfully with `RUSTC_WRAPPER=` (the configured `sccache` is not permitted in this sandbox), then ran the full workspace suite: all 204 tests passed against the current CLI mutation. This confirms F1 is not caught by the existing suite.
- The bounded fixture froze and verified successfully; the overclaim fixture was refused by `packet` with exit 1 and ten blockers. Packet creation therefore did not discard the blockers exercised by those fixtures.
- Probed a copied bounded vault with `quantifier:` removed. BAIMA's `Unassessed` result reached `gates`, and `packet` refused the claim; no discarded no-verdict remained in the current `examine_graph` implementation.
- Traced the production callers of `evidential_closure`, `withdrawn_attacks`, `foreign_title`, `asserted_text`, `supersedes_target`, and `is_exempt_from_quantifier`. The consolidated helpers are used at their relevant call sites; I found no surviving inline version with divergent behavior.
- Inspected the grounded-extension, withdrawal fixed-point, and defender-closure walks, including attack and retraction cycles. The core graph test group (11 tests) passed; the implementations use finite monotone fixed points and ordered sets, and I did not reproduce insertion-order dependence or nontermination.
- Reviewed whole-word, attribution/hedge, party-name, negation, and quantifier exemptions in `lens/src/lints.rs`. The recent boundary and case-normalization handling covered the ordinary-prose collisions I probed; I did not reproduce an additional false refusal or false clearance.
