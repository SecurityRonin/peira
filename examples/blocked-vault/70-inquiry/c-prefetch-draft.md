---
id: c-prefetch-draft
type: claim
title: The Prefetch file proves that the user executed Notepad
aspect: function
quantifier: singular
causal_rung: counterfactual
uses_term: ["exec-01"]
---

The .pf is present with a run count of at least one, therefore the user ran Notepad.

This is the draft the author started from — the sentence `peira_propose` is shown flagging in
`../prefetch-vault`. It over-claims in three separable ways, and peira refuses to freeze it
until each is answered. Fixing them is what turns it into the bounded claim next door.
